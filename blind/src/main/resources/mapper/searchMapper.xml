<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ksinfo.blind.test.mapper.TestMapper">
<select id="findTestData" resultType="com.ksinfo.blind.test.dto.TestDto"> 
    SELECT board_topic_name FROM blind.board_mgt; <!-- 정상적으로 데이터 로드&출력 테스트위해 실시. 정상적으로 출력되면 삭제예정.-->
</select>
</mapper>
